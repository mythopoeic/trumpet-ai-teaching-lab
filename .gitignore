# Python
__pycache__/
*.pyc

# Environment
.env
.venv/

# Database (ChromaDB vector store — binary files regenerated from ingestion)
*.sqlite3
teaching-engine/data/vector_db/

# Testing artifacts
test-output.json
teaching-engine/tests/evaluation/results/

# Session data (runtime)
teaching-engine/data/sessions/

# Windows artifacts
NUL

# Large media files (transcriptions/metadata are kept, raw audio/video excluded)
teaching-engine/data/sources/media/raw/
teaching-engine/data/sources/audio/
teaching-engine/data/models/*
!teaching-engine/data/models/dp_heuristic_thresholds.json
!teaching-engine/data/models/dp_goertzel_calibration.json

# Scraped forum data (raw HTML + parsed JSON — regenerated by scripts)
teaching-engine/data/sources/forums/

# Double-pedal ML dataset: raw audio + regenerated processed/ artifacts
# (feature cache, per-segment WAVs, spectrograms) and regenerated splits.
# split_overrides.csv is the hand-curated, version-controlled split source.
teaching-engine/data/double_pedal_v1/raw/
teaching-engine/data/double_pedal_v1/processed/
teaching-engine/data/double_pedal_v1/splits/*
!teaching-engine/data/double_pedal_v1/splits/split_overrides.csv
teaching-engine/data/ab_comparison/

# ML checkpoints (regenerated by train/ scripts)
teaching-engine/models/

# Runtime upload storage
teaching-engine/uploads/

# Tools
teaching-engine/services/rag/yt-dlp-standalone.exe

# Ralph agent logs/archives
scripts/ralph/.ralph-iteration-*.log
scripts/ralph/archive/

# IDE / agent
.claude/

# Node deps for .sandcastle orchestration
node_modules/

# Workspace tooling that lives at the repo root post-flatten (kept out of the repo,
# as it was before the wrapper folder was removed)
.agents/
skills-lock.json
training samples/

# ============================================================================
# Private / copyrighted corpus — PURGED from history (2026). MUST NOT be re-added.
# This is a public portfolio repo: raw instructional sources, page scans,
# recordings, transcripts, and generated indexes are kept private/local only.
# ============================================================================
# Whole source corpora (books, page scans, recordings, transcripts, renders)
teaching-engine/data/sources/
teaching-engine/services/rag/data/sources/
teaching-engine/teachers/*/sources/
# Generated vector index (built from the private corpus)
teaching-engine/services/rag/vector_db/
# Evaluation material derived from copyrighted books
teaching-engine/tests/evaluation/reference_material/
teaching-engine/tests/evaluation/golden_test_set.json
# Copyrighted documents
*.pdf
*.docx
# Audio / video (recordings, demos, practice artifacts)
*.mp3
*.wav
*.m4a
*.m4v
*.flac
# Transcripts / diarization / index artifacts
*transcript*.json
*diarization*.json
*.pickle
# Env files (keep the example)
.env.*
!.env.example
# OS cruft
.DS_Store

# Curated synthetic / rights-cleared sample data is allowed
!sample_data/
!sample_data/**

# Internal autonomous-agent workspace (not part of the public snapshot)
.sandcastle/
