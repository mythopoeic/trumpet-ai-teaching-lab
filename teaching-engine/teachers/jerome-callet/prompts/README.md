# Prompts (portfolio snapshot)

These files are **skeletons**. Production prompt content that encodes the actual
Jerome Callet-inspired teaching behavior is withheld from the public repository.
They illustrate the prompt-assembly *architecture* only (shared layer + per-era
layer + retrieved-context injection).
