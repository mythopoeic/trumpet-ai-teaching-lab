<!-- PORTFOLIO SNAPSHOT: production prompt content withheld. -->

# General / cross-era prompt — skeleton

Fallback prompt when no single era is selected. Production content withheld.
Same structure as an era prompt (role -> context -> source-grounding -> format),
without era-specific specifics.
