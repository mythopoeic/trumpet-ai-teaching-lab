"""Lesson (teaching) layer - EXCLUDED from this portfolio snapshot.

The stateful lesson-mode product (curriculum / lesson-track composition,
proficiency tracking, drill & exercise selection, placement, and the
audio-driven teaching loop) is proprietary and is intentionally omitted from
the public repository. See ``docs/portfolio-snapshot.md`` for what is excluded
and why.

The retained, publicly-visible engineering is the knowledge/RAG path
(``app.api.routes.chat`` + ``services.rag``), the audio feature-extraction
skeletons (``services.audio``), and the evaluation harness structure.
"""
